{
    "name": "transcript-notes-server",
    "version": "1.0.0",
    "description": "Transcript Notes Server with Vosk for audio transcription",
    "main": "vosk-server.js",
    "scripts": {
        "start": "node vosk-server.js",
        "dev": "nodemon vosk-server.js",
        "install-deps": "npm install && cd react-transcript-notes && npm install --legacy-peer-deps",
        "build": "cd react-transcript-notes && npm run build"
    },
    "dependencies": {
        "axios": "^1.6.2",
        "cors": "^2.8.5",
        "dotenv": "^16.3.1",
        "express": "^4.18.2",
        "multer": "^1.4.5-lts.1",
        "openai": "^4.86.2"
    },
    "devDependencies": {
        "nodemon": "^3.0.1"
    }
}
